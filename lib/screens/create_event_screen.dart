// Deprecated stub to avoid analyzer errors. Implementation moved to `create_event_screen_v2.dart`.
import 'package:flutter/widgets.dart';

class CreateEventScreenDeprecated extends StatelessWidget {
  const CreateEventScreenDeprecated({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

class CreateEventScreen extends StatelessWidget {
  static const routeName = '/create';
  const CreateEventScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // Deprecated: implementation moved to `CreateEventScreenV2`.
    return const SizedBox.shrink();
  }
}
